import media from './media';
import mixins from './mixin';

const colors = {
  white: '#fff',
  black: '#000',
  gray: '#ccc',
  sepia: ' #f0e8d1',
};

export const theme = {
  colors,
  media,
  mixins,
};
